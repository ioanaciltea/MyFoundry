# Libra Assist — Admin Console

## How to Run

1. Ensure the RAG Teaching API backend is running locally at `http://localhost:7799`.
2. Open `index.html` directly in any web browser (or use VS Code Live Server).
3. Click "Check Health Status" to verify the connection to the backend.